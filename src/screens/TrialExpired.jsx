// LootLedger — TrialExpired placeholder.
// Stage 1.A foundation lands the route stub; the actual content
// (sign-out button, contact-us instructions, no-escape gate) is
// filled in by Commit 4.

import React from "react";
export default function TrialExpired(){
  return <div style={{padding:40,fontFamily:"system-ui",color:"#666"}}>
    Trial expired (placeholder — real content lands in Commit 4).
  </div>;
}
