// LootLedger — AdminPanel placeholder.
// Stage 1.A foundation lands the route stub; the shop list + the
// activate/deactivate flow are filled in by Commit 4.

import React from "react";
export default function AdminPanel(){
  return <div style={{padding:40,fontFamily:"system-ui",color:"#666"}}>
    Admin panel (placeholder — real content lands in Commit 4).
  </div>;
}
